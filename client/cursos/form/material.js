   /* var manager =new SubsManager();

        Template.material.onCreated(function(){
            self =this;
             self.readyFiles = new ReactiveVar(false);
            self.autorun(function(){
            var hadler = manager.subscribe("getFiles");
              self.readyFiles.set(hadler.ready()) ;
        
    });
 });*/